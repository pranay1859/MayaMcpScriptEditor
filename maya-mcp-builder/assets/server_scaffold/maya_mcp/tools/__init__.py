"""Tool modules. Each module decorates functions with ``@mcp.tool()``.

Adding a new module:
    1. Create ``my_module.py`` next to this file.
    2. Define tools with ``@mcp.tool()`` decorators.
    3. Add the import to ``maya_mcp.server._load_tools``.
"""
